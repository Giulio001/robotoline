{
  "name": "factory-automation-game",
  "version": "1.0.0",
  "private": true,
  "description": "Un piccolo clone browser di Factorio: estrazione, nastri trasportatori e crafting automatizzato",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/game.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
